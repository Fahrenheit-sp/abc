name: ABC
options:
  bundleIdPrefix: com.fahrenheit
targets:
  ABC:
    type: application
    platform: iOS
    deploymentTarget: "12.0"
    sources: [ABC]
    preBuildScripts:
      - script: swiftgen
        name: Swiftgen
        inputFiles:
          - $(SRCROOT)/swiftgen.yml
    settings:
      DEVELOPMENT_TEAM: 66W9KV8MC3
