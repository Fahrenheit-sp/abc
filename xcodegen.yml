name: ABC
options:
  bundleIdPrefix: com.fahrenheit
targets:
  ABC:
    type: application
    platform: iOS
    deploymentTarget: "11.0"
    sources: [ABC]
    preBuildScripts:
      - script: swiftgen
        name: Swiftgen
        inputFiles:
          - $(SRCROOT)/swiftgen.yml
